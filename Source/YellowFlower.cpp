#include "YellowFlower.h"

/*YellowFlower::YellowFlower(SDL_Rect r, int flag, bool destructible, Collider* collider, SDL_Texture* texture) : Obstacle(r, flag, destructible, collider, texture) 
{

	this->SetTexture("Assets/Images/Sprites/Environment_Sprites/Yellow_Flower.png");
	destructible = true;
	collider = new Collider(r, Collider::Type::WALL);

	idle.PushBack({ 9,8,16,16 });

}
*/
